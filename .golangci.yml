version: "2"
run:
  tests: true
linters:
  enable:
    - revive
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/abraira85/pork
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
